# Cloud SQL Configuration (Public IP method)
# Replace YOUR_CLOUD_SQL_PUBLIC_IP with actual public IP from Cloud SQL instance
DATABASE_URL=postgresql://loanuser:loanpass@YOUR_CLOUD_SQL_PUBLIC_IP:5432/loandb

# Google Cloud Storage Configuration
GCS_BUCKET_NAME=project-dev-496407-loan-uploads
GCP_PROJECT_ID=project-dev-496407
USE_CLOUD_STORAGE=true

# Local Storage (for local development only)
UPLOAD_DIR=uploads

# API Configuration
SECRET_KEY=supersecret-money-lending-key-2024
DEFAULT_INTEREST_RATE=3.0
